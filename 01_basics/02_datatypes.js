"use strict";//  treat all js code as newer version

// alert(3+3) // we are using notejs not browser

console.log(3+
    3)// code readability should be high

    console.log("dhiraj")

    let name="dhiraj"
    //let age= 14
    let age= null
    let isLoggedIn = false

    //number = 2 to power 53
    //bigint 
    // String == " "
    //boolean == true or false
    //null is a standalone value ( representation of a empty value)
    // undefined ==> declare but not defined
    // symbol ==> unique


    // object


    console.log(typeof "dhiraj");
    console.log(typeof null);// as null is an object 
    console.log(typeof undefined);// undefined is an datatype so it prints the same